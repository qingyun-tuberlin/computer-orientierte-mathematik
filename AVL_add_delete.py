"""
def addNode(node):
    add node
    while(after inserting/modifying the node,
    there exists the smallest not balanced tree)
        ajust tree according to cases: LL,RR, LR,RL

def deleteNode(node):
    delete the node
    while(check from bottom to top 
        && there exists the smallest not balanced tree)
            find the tallest son and grandson
            adjust the tree according to cases: LL,RR, LR,RL
    
"""